version: '3.8'

services:
  # ===================== INFRASTRUCTURE =====================

  postgres:
    image: postgres:18-alpine
    container_name: kapila-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    ports:
      - "5432:5432"
#    networks:
#      - kapila-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
#
#  redis:
#    image: redis:7-alpine
#    container_name: kapila-redis
#    restart: unless-stopped
#    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 256mb --maxmemory-policy allkeys-lru
#    volumes:
#      - redis_data:/data
#    ports:
#      - "6379:6379"
#    networks:
#      - kapila-network
#    healthcheck:
#      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#  zookeeper:
#    image: confluentinc/cp-zookeeper:7.6.0
#    container_name: kapila-zookeeper
#    restart: unless-stopped
#    environment:
#      ZOOKEEPER_CLIENT_PORT: 2181
#      ZOOKEEPER_TICK_TIME: 2000
#    volumes:
#      - zookeeper_data:/var/lib/zookeeper/data
#      - zookeeper_log:/var/lib/zookeeper/log
#    networks:
#      - kapila-network
#    healthcheck:
#      test: ["CMD", "nc", "-z", "localhost", "2181"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#  kafka:
#    image: confluentinc/cp-kafka:7.6.0
#    container_name: kapila-kafka
#    restart: unless-stopped
#    depends_on:
#      zookeeper:
#        condition: service_healthy
#    environment:
#      KAFKA_BROKER_ID: 1
#      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
#      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
#      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
#      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
#      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
#      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
#      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
#      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
#      KAFKA_LOG_RETENTION_HOURS: 168
#    volumes:
#      - kafka_data:/var/lib/kafka/data
#    ports:
#      - "9092:9092"
#    networks:
#      - kapila-network
#    healthcheck:
#      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
#      interval: 30s
#      timeout: 10s
#      retries: 5
#      start_period: 30s
#
#  kafka-ui:
#    image: provectuslabs/kafka-ui:latest
#    container_name: kapila-kafka-ui
#    depends_on:
#      - kafka
#    environment:
#      KAFKA_CLUSTERS_0_NAME: kapila
#      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
#      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
#    ports:
#      - "9000:8080"
#    networks:
#      - kapila-network
#
#  minio:
#    image: minio/minio:latest
#    container_name: kapila-minio
#    restart: unless-stopped
#    environment:
#      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
#      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
#    command: server /data --console-address ":9001"
#    volumes:
#      - minio_data:/data
#    ports:
#      - "9090:9000"
#      - "9001:9001"
#    networks:
#      - kapila-network
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
#      interval: 30s
#      timeout: 20s
#      retries: 3

volumes:
  postgres_data:
#  redis_data:
#  zookeeper_data:
#  zookeeper_log:
#  kafka_data:
#  minio_data:
